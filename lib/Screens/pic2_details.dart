import 'package:flutter/material.dart';

class Pic2Details extends StatelessWidget {
  const Pic2Details({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}