import 'package:flutter/material.dart';

class Pic3Details extends StatelessWidget {
  const Pic3Details({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}